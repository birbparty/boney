# Root nim.cfg — applies to all targets including editor LSP checks.
# Mirrors the console target profile (arc + useMalloc) so desktop builds
# catch the same memory model violations as 3DS/Vita builds.
--path:"src"
--mm:arc
--define:useMalloc
